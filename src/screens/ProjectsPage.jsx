import {Container} from "@mui/material";

// Todo: make a method to map projects over here.
//  Check the function details in projectDataService.js, and appropriately call it here and map the data fetched. (hint: useEffect).
//  If there is an error use the error message component (in components) to display the error properly


function ProjectsPage() {

    // TODO: Make sure project return is within a gallery like view (check out material UI interfaces and add own CSS for better design if you can ).
    //  add other things beyond just the project too though at the top
    return (<Container size='lg'>

    </Container>);
}

export default ProjectsPage;